servers:
    type: string
    value: "+*"
    lable: Servers
    description: "Comma separated list of NTP servers to get time"
package_ensure:
    type: list
    value: {present, absent}
    lable: "Package status"
    description: "NTP package status"